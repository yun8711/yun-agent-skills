---
description: kd-fe 项目代码风格、规范和约定
alwaysApply: true
---

# kd-fe 项目代码规范

## 代码风格

### 基本格式

- **引号**：使用双引号（`"`）
- **缩进**：2 个空格
- **分号**：行尾必须加分号
- **末尾逗号**：多行时保留末尾逗号
- **行宽**：最多 120 字符
- **换行符**：使用 LF（`\n`）

## 命名规范

- **组件名**：PascalCase（如 `UserCard`、`BaseTable`）
- **文件名**：kebab-case（如 `user-card.vue`、`base-table.vue`）
- **变量/方法名**：camelCase（如 `userName`、`handleClick`、`queryList`）
- **CSS 类名**：kebab-case（如 `.user-card`、`.table-wrapper`）
- **常量**：UPPER_SNAKE_CASE（如 `MAX_COUNT`、`API_BASE_URL`）

## Vue 组件结构

### 文件结构顺序

严格按照以下顺序组织：

1. **template** - 模板部分
2. **script** - 脚本部分
3. **style** - 样式部分（必须使用 `scoped` 和 `lang="scss"`）

### Script 部分顺序

```javascript
export default {
  name: "ComponentName",        // 1. 组件名（PascalCase）
  components: {},                // 2. 局部组件注册
  mixins: [],                    // 3. Mixin
  props: {},                     // 4. Props 定义
  data() {},                     // 5. 数据
  computed: {},                  // 6. 计算属性
  watch: {},                     // 7. 监听器
  created() {},                  // 8. 生命周期钩子（按执行顺序）
  mounted() {},
  methods: {},                   // 9. 方法
};
```

### Props 定义规范

使用对象形式，必须包含 `type` 和 `default`：

### Vue 模板规范

- 标签自闭合：void 元素（如 `img`、`input`）必须自闭合，普通元素和组件不能自闭合
- 属性换行：单行时第一个属性不换行，多行时第一个属性换行
- 多属性换行：单行最多 10 个属性，多行时每行 1 个属性

## 项目约定

### 禁止直接修改 Props

为了保证单向数据流，禁止在组件内直接修改 props：

### 响应式数据

使用 `this.$set` 或 `Vue.set` 添加响应式属性：

### 性能优化

使用 `Object.freeze` 冻结不需要响应式的数据（如枚举、常量配置）：

### 路由跳转

```javascript
// 普通跳转
this.$router.push({ name: "PageName", query: { id: "123" } });
```

### 防抖和节流

使用 lodash 的 debounce/throttle，并在组件销毁时取消：

```javascript
import debounce from "lodash/debounce";

export default {
  data() {
    return {
      debouncedSearch: null,
    };
  },
  mounted() {
    this.debouncedSearch = debounce(this.queryList, 500);
  },
  beforeDestroy() {
    this.debouncedSearch && this.debouncedSearch.cancel();
  },
  methods: {
    handleSearch() {
      this.debouncedSearch();
    },
  },
};
```

## 错误处理

```javascript
// ❌ BAD
try {
  await fetchData();
} catch (e) {}

// ✅ GOOD
try {
  await fetchData();
} catch (e) {
  console.error("Failed to fetch data", e);
  this.$message.error("获取数据失败");
}
```

## 代码质量

- 使用 `===` 和 `!==` 进行比较（字面量、typeof、与 null 比较除外）
- 未使用的变量应该删除或注释说明原因
- 生产环境禁用 `console` 和 `debugger`
- 使用 `const` 声明未被重新赋值的变量
