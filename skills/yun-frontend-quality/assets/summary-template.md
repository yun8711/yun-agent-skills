# Step 4 汇总模板

按用户已选填写。确认前不写盘。

```markdown
## 项目画像（已确认）
- 框架 / 构建器 / 包管理器：
- 根目录：是（第一期只配根）

## 本轮能力
- 勾选：
- 本轮不做：

## 工具
- 格式化：
- JS/TS：
- CSS：
- ESLint 配置格式（若有）：
- 集成：

## 风格表
- 引号 / 分号 / 尾逗号 / 行宽 / 缩进：
- 模板 `>` / 属性换行（若问了）：
- commitlint / tsconfig / npmrc（若问了）：

## 将安装的依赖
- （包名，不写死补丁号）

## 将改动的文件
| 路径 | 新建或覆盖 | 说明 |
|------|------------|------|

## 派生文件
- ignore / editorconfig / gitattributes / extensions.json：生成或跳过（随覆盖策略）

## 覆盖策略（用户已选）
- 全部覆盖 / 已有的跳过 / 逐文件

## 验证
- 烟雾：1～2 个样例文件 dry-run；不全仓 --fix
```
